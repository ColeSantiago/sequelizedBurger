<div class="wrapper">
  <h1>Eat-Da-Burger!</h1>

  <div class="burger-photo-div"><img class="burger-photo" src="burger.png"></div>

      <ul class="not-eaten">
        {{#each burger}}
          {{#unless devoured}}
          <li>
            {{burger_name}}

              <div class="guest-wrapper">
                  <form class="guest-form">
                    <div class="guest-group">
                      <input type="text" id="guest-name" name="guest-name" placeholder="Guest Name">
                    </div>
                  </form>
              </div>

            <button class="eat-burger" data-id="{{id}}" data-newDevour="{{devoured}}" id="{{devoured}}" type="submit">
              {{#if devoured}}{{else}}DEVOUR IT!{{/if}} 
            </button>
          </li>    
          {{/unless}}
        {{/each}}
      </ul>
    
    <div class="eaten">
      <ul class="eaten-burgers">
        {{#each burger}}
          {{#if devoured}}
          <li>
            <p>{{this.burger_name}} , eaten by {{guest.guest_name}} </p>
          </li>
          {{/if}}
        {{/each}}
      </ul>
    </div>
  
  <div class="form-wrapper">
    <h2>Add a Burger</h2>
      <form class="create-form">
        <div class="form-group">
          <label for="burger-name">Burger Name:</label>
          <input type="text" id="burger-name" name="name">
        </div>
        <button type="submit" class="add-burger">Add Burger</button>
      </form>
  </div>
</div>